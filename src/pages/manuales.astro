---
import Layout from "../layouts/Layout.astro";
import PdfCard from "../components/PdfCard.astro";
import { dataPdfs } from "../data/documentos.js";

const manualPdfs = dataPdfs.filter(item => item.type === "Manual");

const title = "Manuales PDF"
---


  <Layout title={title}>
    <section class="grid-pdfs">
      {
        manualPdfs.map((item) => (
          <PdfCard
            title={item.title}
            type={item.type}
            size={item.size}
            url={item.url}
          ></PdfCard>
        ))
      }
    </section>
  </Layout>

